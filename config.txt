# ============================
#  TIDL Import Config for AM68PA
#  Model: emotion_cnn.onnx
#  Input: float32[1,3,224,224]
# ============================

# 1 = ONNX model
modelType = 1

# Path to your ONNX model
inputNetFile = ./emotion_cnn.onnx

# Base name for output (TIDL artifacts)
outputNetFile = emotion_tidl

# Quantization style:
# 2 = Calibration (recommended)
quantizationStyle = 2

# Use 1 frame for calibration (OK for small models)
numFrames = 1
calibrationOption = 7

# Input tensor shape
inNumChannels = 3
inHeight = 224
inWidth = 224

# Input layout (ONNX models almost always NCHW)
inDataLayout = NCHW

# Input element type: 2 = float32
# (TIDL will quantize down to 8-bit internally)
inElementType = 2

# Normalization disabled (model expects float32 directly)
inDataNorm = 0

# Debug logs
debugTraceLevel = 1

# Execute network compiler (1 = yes)
executeNetworkCompiler = 1

# Device name for AM68PA SoC
deviceName = 1
